# Browser mode: real by default. npm run dev:mock is an isolated design preview.
VITE_DATA_MODE=real
# Server-side Vite proxy settings; do not prefix the API key with VITE_.
BACKEND_URL=http://127.0.0.1:8000
BACKEND_API_KEY=
